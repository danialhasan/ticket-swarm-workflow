interface:
  display_name: "Ticket Swarm Workflow"
  short_description: "Repo-specific patterns and workflows for ticket-swarm-workflow"
  default_prompt: "Use the ticket-swarm-workflow repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true