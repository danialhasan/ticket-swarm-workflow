# Setup Ticket Swarm Workflow

This directory is a non-runtime placeholder. It is not an active Codex skill
because it does not contain `SKILL.md`.

If this becomes a real workflow, add a `SKILL.md`, assign it to a group under
`../../skill-groups/`, and update `../README.md`.

