# UI Annotation Conveyor

This directory is a non-runtime placeholder. It is not an active Codex skill
because it does not contain `SKILL.md`.

The current active annotation/product-truth skills are:

- `annotation-reconciliation-guard`
- `ui-abstraction-guard`

If this becomes a real workflow, add a `SKILL.md`, assign it to
`../../skill-groups/10-requirements-discovery-and-product-truth/`, and update
`../README.md`.

